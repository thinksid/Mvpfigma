# Netlify/Vercel SPA Redirects Configuration
# This file ensures all routes are handled by the React app (client-side routing)

# Redirect all requests to index.html for client-side routing
# except for files that actually exist
/*    /index.html   200
